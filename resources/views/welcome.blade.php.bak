<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>GhostNote - Notas que se autodestruyen</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
    <style>
        [x-cloak] { display: none !important; }
    </style>
</head>
<body class="bg-gray-900 text-gray-100 min-h-screen flex flex-col items-center justify-center p-4 font-sans antialiased">

    <div class="w-full max-w-2xl bg-gray-800 rounded-xl shadow-2xl overflow-hidden border border-gray-700" x-data="{
        content: '',
        ttl: '',
        max_views: 1,
        loading: false,
    }">
        <div class="p-8">
            <h1 class="text-3xl font-bold mb-2 text-transparent bg-clip-text bg-gradient-to-r from-purple-400 to-pink-600">GhostNote</h1>
            <p class="text-gray-400 mb-8">Crea una nota segura que se autodestruye.</p>

            @if (session('created_url'))
                <div class="mb-8 p-4 bg-green-900/50 border border-green-500 rounded-lg">
                    <p class="text-green-400 font-semibold mb-2">¡Secreto Creado!</p>
                    <div class="flex items-center gap-2">
                        <input type="text" readonly value="{{ session('created_url') }}" class="w-full bg-gray-900 border border-gray-700 rounded px-3 py-2 text-gray-300 focus:outline-none">
                        <button @click="navigator.clipboard.writeText('{{ session('created_url') }}')" class="bg-gray-700 hover:bg-gray-600 text-white px-4 py-2 rounded transition">Copiar</button>
                    </div>
                    <p class="text-xs text-gray-500 mt-2">Comparte este enlace. Se autodestruirá después de ser visto.</p>
                </div>
                <div class="text-center">
                    <a href="{{ route('home') }}" class="text-purple-400 hover:text-purple-300 underline">Crear Otro</a>
                </div>
            @else
                <form action="{{ route('secret.store') }}" method="POST" @submit="loading = true">
                    @csrf
                    
                    <div class="mb-6">
                        <label class="block text-sm font-medium text-gray-400 mb-2">Contenido del Secreto</label>
                        <textarea name="content" x-model="content" rows="6" class="w-full bg-gray-900 border border-gray-700 rounded-lg p-4 text-gray-100 focus:ring-2 focus:ring-purple-500 focus:border-transparent transition" placeholder="Escribe tu secreto aquí..." required></textarea>
                    </div>

                    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
                        <div>
                            <label class="block text-sm font-medium text-gray-400 mb-2">Expiración (Opcional)</label>
                            <select name="ttl" x-model="ttl" class="w-full bg-gray-900 border border-gray-700 rounded-lg p-3 text-gray-100 focus:ring-2 focus:ring-purple-500 focus:border-transparent">
                                <option value="">Nunca (hasta que se vea)</option>
                                <option value="5">5 Minutos</option>
                                <option value="60">1 Hora</option>
                                <option value="1440">1 Día</option>
                                <option value="10080">7 Días</option>
                            </select>
                        </div>
                        <div>
                            <label class="block text-sm font-medium text-gray-400 mb-2">Vistas Máximas</label>
                            <input type="number" name="max_views" x-model="max_views" min="1" class="w-full bg-gray-900 border border-gray-700 rounded-lg p-3 text-gray-100 focus:ring-2 focus:ring-purple-500 focus:border-transparent" required>
                        </div>
                    </div>

                    <button type="submit" :disabled="loading || !content" class="w-full bg-gradient-to-r from-purple-600 to-pink-600 hover:from-purple-500 hover:to-pink-500 text-white font-bold py-4 rounded-lg shadow-lg transform transition hover:-translate-y-0.5 disabled:opacity-50 disabled:cursor-not-allowed">
                        <span x-show="!loading">Crear Enlace Secreto</span>
                        <span x-show="loading" x-cloak>Encriptando...</span>
                    </button>
                </form>
            @endif
        </div>
    </div>
    
    <footer class="mt-12 text-gray-600 text-sm">
        &copy; {{ date('Y') }} GhostNote. Seguro y Efímero.
    </footer>
</body>
</html>
